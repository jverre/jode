{
  "name": "@jode/opencode",
  "private": true,
  "type": "module",
  "description": "jode remote environment — Worker + Durable Object + Container serving the OpenCode web UI, with R2-persisted /workspace (sibling of @jode/claude-code for Claude Code; see plan 02).",
  "scripts": {
    "deploy": "wrangler deploy",
    "dry-run": "wrangler deploy --dry-run",
    "dev": "wrangler dev --port 8788 --inspector-port 9230",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.0.21",
    "@jode/auth": "*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260603.1",
    "typescript": "^5.9.0",
    "wrangler": "^4.20.0"
  }
}
