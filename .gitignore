node_modules/
out/
dist/
*.log
.DS_Store
.env
.env.*

# Cloudflare local state + dev secrets
.wrangler/
.dev.vars

# Regenerable Claude Desktop payload (323MB+). Rebuilt from /Applications/Claude.app
# via `npm run build:payload`. Not committed — see apps/claude-code/README.md.
apps/claude-code/payload/
apps/claude-code/.payload-build/

# Regenerable Codex payload (140MB+ asar). Rebuilt from /Applications/Codex.app
# via `npm run build:payload`. Not committed — see apps/codex/README.md.
apps/codex/payload/
apps/codex/.payload-build/

# Generated by gen-assets.mjs from the (gitignored) payload; regenerated on every
# dev/build/deploy. Not committed — see apps/claude-code/README.md.
apps/*/src/generated-assets.ts
